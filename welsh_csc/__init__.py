"""Package initialisation for the Welsh Controlled Speech Corpus tool."""

if __name__ == "__main__":
    print(
        "To run the Welsh Controlled Speech Corpus tool invoke it as a "
        "module using python -m welsh_csc."
    )